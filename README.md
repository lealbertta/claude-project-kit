# Claude Project Kit

A starting set of agent files for a new project — a short always-loaded
instruction file, path-scoped rules, on-demand skills, review subagents, a
permission posture, and a documentation layer built around independent work items.
